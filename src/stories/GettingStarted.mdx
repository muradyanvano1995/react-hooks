import { Meta } from '@storybook/addon-docs/blocks'

import { CodeBlock } from './components/CodeBlock'
import { HOOK_COUNT } from './docs/catalog'
import { PACKAGE_INSTALL_COMMAND, PACKAGE_NAME } from './docs/packageMetadata'
import { gettingStartedSnippet } from './components/useOnClickOutside.snippets'

<Meta title="Getting Started" />

<div className="sb-unstyled mx-auto max-w-3xl space-y-8 px-2 py-6 font-sans text-slate-900">
  <header className="space-y-3 border-b border-slate-200 pb-6">
    <h1 className="text-3xl font-semibold tracking-tight">Getting started</h1>
    <p className="text-base leading-7 text-slate-600">
      Install <code>{PACKAGE_NAME}</code> from npm and explore the{' '}
      {HOOK_COUNT} public hooks in this Storybook.
    </p>
  </header>

<section className="space-y-3">
  <h2 className="text-xl font-semibold">Installation</h2>
  <CodeBlock code={PACKAGE_INSTALL_COMMAND} language="bash" />
  <p className="text-sm leading-7 text-slate-600">
    Hosted documentation:{' '}
    <a
      className="font-medium text-indigo-700 underline-offset-2 hover:underline"
      href="https://muradyanvano1995.github.io/react-hooks/"
      rel="noreferrer"
      target="_blank"
    >
      muradyanvano1995.github.io/react-hooks
    </a>
  </p>
</section>

<section className="space-y-3">
  <h2 className="text-xl font-semibold">TypeScript and React</h2>
  <ul className="list-disc space-y-2 pl-5 text-sm leading-7 text-slate-600">
    <li>Written in TypeScript with strict library settings.</li>
    <li>
      Peer range: <code>react@^18.0.0 || ^19.0.0</code>.
    </li>
    <li>ESM-only. No CommonJS export map.</li>
    <li>
      Runtime dependency: <code>qrcode</code> (used by <code>useQRCode</code>{' '}
      only).
    </li>
  </ul>
</section>

<section className="space-y-3">
  <h2 className="text-xl font-semibold">SSR and React Server Components</h2>
  <ul className="list-disc space-y-2 pl-5 text-sm leading-7 text-slate-600">
    <li>
      Importing the package does not access <code>window</code> or{' '}
      <code>document</code> at module evaluation time.
    </li>
    <li>
      Hooks must be called from Client Components when using React Server
      Components.
    </li>
    <li>
      Browser subscriptions such as document listeners and{' '}
      <code>MutationObserver</code> are attached in effects, so server rendering
      does not register them.
    </li>
  </ul>
</section>

<section className="space-y-3">
  <h2 className="text-xl font-semibold">Minimal example</h2>
  <CodeBlock code={gettingStartedSnippet} language="tsx" />
  <p className="text-sm leading-7 text-slate-600">
    Keep the trigger inside the referenced element when using the default{' '}
    <code>pointerdown</code> listener so opening does not immediately close.
  </p>
  <p className="text-xs leading-5 text-slate-500">
    Example styling in Storybook uses Tailwind for documentation only. The hooks
    package does not require Tailwind.
  </p>
</section>

<section className="space-y-3">
  <h2 className="text-xl font-semibold">Where to go next</h2>
  <ul className="list-disc space-y-2 pl-5 text-sm leading-7 text-slate-600">
    <li>
      Use the Introduction catalog to open any hook&apos;s Documentation page.
    </li>
    <li>
      Read the repository{' '}
      <a
        className="font-medium text-indigo-700 underline-offset-2 hover:underline"
        href="https://github.com/muradyanvano1995/react-hooks/blob/main/docs/public-api.md"
        rel="noreferrer"
        target="_blank"
      >
        public API reference
      </a>{' '}
      for signatures and types.
    </li>
    <li>
      Review SSR, storage, media, and browser-API guides under{' '}
      <code>docs/guides/</code> in the repository.
    </li>
  </ul>
</section>
</div>
